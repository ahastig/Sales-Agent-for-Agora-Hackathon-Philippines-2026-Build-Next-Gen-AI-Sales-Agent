PORT=8787
CORS_ORIGIN=http://localhost:5173
DATABASE_PATH=data/sales-agent.sqlite

# Production database. If set, the backend uses PostgreSQL instead of SQLite.
# Use a free Postgres provider such as Neon or Supabase, then paste the connection string here.
DATABASE_URL=
DATABASE_SSL=true

# Optional: use any OpenAI-compatible chat-completions provider.
OPENAI_API_KEY=
OPENAI_MODEL=gpt-4o-mini
OPENAI_BASE_URL=https://api.openai.com/v1
